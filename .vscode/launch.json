{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Grunt Test",
            "args": ["test"],            
            "program": "${workspaceRoot}/node_modules/grunt-cli/bin/grunt",
            "stopOnEntry": true,
            "cwd": "${workspaceRoot}"
        }
    ]
}